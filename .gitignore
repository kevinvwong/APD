# Next.js
.next/
node_modules/
tsconfig.tsbuildinfo

# Environment — never commit real credentials
.env
.env.local
.env*
!.env.example

# Vercel
.vercel

# Claude Code session data
.claude/

# Python (FM acquisition scripts)
__pycache__/
*.pyc
*.pdf

# Generated — rebuild with: npm run search:index
# src/data/search-index.json  ← committed intentionally so deploys work without DB at build time

# Design handoff assets — large binaries, not for version control
design_handoff_doctrine_library/
